browser as object
b = Browser()

cover all major elements and action on them
b.button("id").click
b.input("id").set("value")
b.html

useful methods
b.save_html(f)
b.html
b.take_screenshot
some useful form stuff

default behavior:
wait time
wait till document ready ?
ways for user to override such behavior

browser pool - thread safe

easy logging
better exception handling
include requests ?
response as object

